apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.conv_api.name }}-clusterip
spec:  
  type: ClusterIP    
  ports:
  - port: {{ .Values.conv_api.port }}
    protocol: TCP
  selector:
    components: {{ .Values.conv_api.components }}